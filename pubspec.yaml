name: flutter_qlkho_new
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # --- CÁC THƯ VIỆN CŨ ---
  http: ^1.2.0
  image_picker: ^1.1.2
  cupertino_icons: ^1.0.8
  fl_chart: ^0.65.0
  provider: ^6.1.2
  mobile_scanner: ^5.1.0
  shared_preferences: ^2.3.2
  qr_flutter: ^4.1.0
  vietqr_flutter: ^1.0.1

  # --- CÁC THƯ VIỆN FIREBASE ---
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  google_sign_in: ^6.2.1
  firebase_messaging: ^15.1.3
  
  # [MỚI] Thư viện Database cho Chat
  cloud_firestore: ^5.4.4
  
  # [MỚI] Thư viện xử lý ngày giờ (để hiển thị giờ chat)
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true